 You are running a deep, evidence-backed analysis of this repository. Produce a structured report. Follow these rules strictly. If you cannot follow a rule,
  STOP and tell me which rule and why — do not proceed.

 # Hard Rules

 1. CITE EVERYTHING. Every factual claim must end with `(path/to/file.ts:LINE)` using a real line number. No file:line → claim is invalid → omit it or mark
 `UNVERIFIED`.
 2. NO SPECULATION. Banned phrases: "probably", "likely", "I think", "I assume", "it seems", "if I had to guess", "typically", "usually", "should be". If
 you would write one of these, instead write `UNVERIFIED: <question>` in the "Open Questions" section.
 3. NO FABRICATION. Do not invent module names, function names, file paths, line numbers, env vars, or commands. If you have not read it, you do not know
 it.
 4. READ BEFORE CLAIMING. You may not describe a file you have not opened with the Read tool in this session.
 5. FAIL LOUDLY. If a section cannot be filled with cited evidence, write `INSUFFICIENT EVIDENCE — files inspected: [list]` instead of guessing.

 # Required Reading Order (do these BEFORE anything else)

 1. Read `CLAUDE.md` at repo root — full file.
 2. Read `graphify-out/GRAPH_REPORT.md` — full file.
 3. If `graphify-out/wiki/index.md` exists, read it. Otherwise list `graphify-out/wiki/` and read up to 5 top-level pages.
 4. Read `MAIN.md`, `MAIN_FEATURES.md`, `CODING_ROADMAP.md`, `HOW_TO.md` from repo root.
 5. Read `apps/backend/src/app.module.ts` and `apps/backend/src/main.ts`.
 6. Read `apps/backend/prisma/schema.prisma`.
 7. Read `apps/frontend/src/App.tsx` and `apps/frontend/vite.config.js`.
 8. List `apps/backend/src/` and `apps/frontend/src/context/` and `apps/frontend/src/pages/` with Glob.

 If any required file is missing, STOP and report which one.

 # Tools You Must Use

 - `Skill` tool with `graphify` if cross-module question arises ("how does X relate to Y").
 - `Agent` with `subagent_type: code-explorer` for tracing a feature end-to-end across 3+ files.
 - `Grep` (NOT bash grep/rg) for symbol lookup.
 - `Glob` for file enumeration.
 - `Read` for every file you cite.

 Do not use `Bash` for searching. Do not use the WebSearch tool — this is a private repo analysis.

 # Output Format (exact section order, exact headings)

 ## 1. Repo Map
 Tree of top-level dirs (depth 2). Each leaf annotated with one-line purpose + citation.

 ## 2. Backend Architecture
 - NestJS modules in registration order from `app.module.ts` (cite line per module).
 - Per module: responsibility (1 sentence) + key service file + key controller file.
 - Global concerns: auth strategy, CSRF, CORS, rate limiting, API versioning — each cited.

 ## 3. Frontend Architecture
 - Routing entrypoint with route table (path → component → layout) from `App.tsx`.
 - Context providers in `src/context/` — name, state owned, mount scope.
 - API client setup (`src/lib/api.ts`) — baseURL, interceptors, auth header strategy.

 ## 4. Data Model
 - Every `model` block in `schema.prisma` (name + line range).
 - Enums.
 - Relations diagram in ASCII (only relations actually declared in schema).

 ## 5. Critical Data Flows
 Pick 3 flows. Trace each with file:line per step:
 - Public customer places an order from QR menu → payment → kitchen.
 - Owner logs in → loads dashboard → views live tables.
 - Stripe Connect onboarding → webhook → restaurant marked enabled.

 ## 6. Realtime / Sockets
 - Gateway file, events emitted, who listens, how frontend reconnects.

 ## 7. SaaS Tiering (if present)
 - Enum values, where tier is read on each request, where it gates features (backend + frontend).
 - Mark "NOT FOUND" if any layer missing.

 ## 8. Security Posture
 Only what is *actually configured* in code:
 - AuthN method, token storage, CSRF, CORS origins, helmet, rate limits, secret loading.
 Do NOT recommend improvements in this section.

 ## 9. Test Coverage Reality
 - Test runner config files (Jest/Vitest configs).
 - Count `*.spec.ts` / `*.test.ts(x)` files per area via Glob.
 - E2E setup status.
 - DO NOT claim a coverage %. State "coverage % not measured in this session" unless you ran the coverage command.

 ## 10. Shipped vs Planned
 Reproduce from `CLAUDE.md` "Shipped" sections + `CODING_ROADMAP.md`. Cite section headings. No interpretation.

 ## 11. Risks & Debt
 Only items explicitly flagged in code comments (`TODO`, `FIXME`, `HACK`, `XXX`) or in `CLAUDE.md` "gotchas" / "do not" sections. Cite each. No invented
 risks.

 ## 12. Open Questions (UNVERIFIED)
 Bulleted list of every `UNVERIFIED: ...` marker you accumulated. This section must be non-empty — if empty, you skipped Rule 2.

 # Stop Conditions

 End the report immediately if:
 - Context window approaches 85% full → emit "PARTIAL REPORT — stopped at section N for context safety" and list completed sections.
 - A required file cannot be read → emit "BLOCKED on <file>" and stop.

 Begin now. Start with Required Reading Order step 1.