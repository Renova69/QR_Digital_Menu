diff --git a/apps/frontend/src/context/CartContext.tsx b/apps/frontend/src/context/CartContext.tsx
--- a/apps/frontend/src/context/CartContext.tsx
+++ b/apps/frontend/src/context/CartContext.tsx
@@ -25,7 +25,6 @@ export interface CartItem {
   quantity: number;
   selectedOptions: SelectedOption[];
   itemTranslations?: MenuTranslationMap | null;
-  rewardPointsPrice?: number;
 }
 
 // Define what the CartContext provides
diff --git a/apps/frontend/src/pages/CheckoutPage.tsx b/apps/frontend/src/pages/CheckoutPage.tsx
--- a/apps/frontend/src/pages/CheckoutPage.tsx
+++ b/apps/frontend/src/pages/CheckoutPage.tsx
@@ -124,7 +124,7 @@ const CheckoutPage = () => {
   const finalMultiplier = Math.max(hhMultiplier, tierMultiplier);
 
   const getItemsPointsCost = () => {
-    return items.reduce((sum, item) => {
+    return items.reduce((sum, item: any) => {
       if (redeemedCartIds.has(item.cartId) && item.rewardPointsPrice) {
         return sum + item.rewardPointsPrice * item.quantity;
       }
@@ -563,7 +563,7 @@ const CheckoutPage = () => {
                     </ul>
                   )}
-                  {item.rewardPointsPrice && user && (
+                  {(item as any).rewardPointsPrice && user && (
                     <>
                       <button
                         onClick={() => {
@@ -575,7 +575,7 @@ const CheckoutPage = () => {
                             });
                           } else if (
                             loyaltyPoints - getItemsPointsCost() >=
-                            (item.rewardPointsPrice ?? 0) * item.quantity
+                            (item as any).rewardPointsPrice * item.quantity
                           ) {
                             setRedeemedCartIds((prev) => {
                               const next = new Set(prev);
@@ -603,7 +603,7 @@ const CheckoutPage = () => {
                           ? t("checkout.redeemedFree")
                           : t("checkout.redeemForPts", {
                               pts:
-                                (item.rewardPointsPrice ?? 0) * item.quantity,
+                                (item as any).rewardPointsPrice * item.quantity,
                             })}
