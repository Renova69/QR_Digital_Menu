# QR Digital Menu — Full Repository Audit

**Date:** 2026-05-16  
**Auditor:** Claude Code (repo-audit skill)  
**Source files read this session:** CLAUDE.md, graphify-out/GRAPH_REPORT.md, MAIN.md, CODING_ROADMAP.md, apps/backend/src/app.module.ts, apps/backend/src/main.ts, apps/backend/prisma/schema.prisma, apps/frontend/src/App.tsx, apps/frontend/vite.config.js, apps/frontend/src/lib/api.ts, apps/backend/src/events/events.gateway.ts, apps/backend/src/subscription/feature-flag.enum.ts, apps/backend/src/subscription/feature.service.ts, apps/backend/src/subscription/subscription.service.ts, apps/backend/src/subscription/subscription.controller.ts, apps/frontend/src/hooks/useFeature.ts, apps/backend/src/loyalty/loyalty.service.ts (partial)

---

## 1. Repo Map

```
QR_Digital_Menu-main/
├── apps/
│   ├── backend/           NestJS 11 + Prisma 6 API on :3000, Swagger at /api-docs
│   │   ├── src/           18 NestJS modules (see section 2)
│   │   ├── prisma/        schema.prisma (16 models, 10 enums), migrations/, seed.ts
│   │   └── test/          jest-e2e.json — E2E test config
│   └── frontend/          Vite + React 18 on :3001, strictPort
│       └── src/
│           ├── context/   9 React Context providers (see section 3)
│           ├── pages/     31 page components (see section 3)
│           ├── components/ UI primitives, POS, menu, staff, public menu
│           ├── hooks/     useFeature.ts, useFocusTrap.ts, etc.
│           └── lib/       api.ts (axios client), currency.ts (EUR/BGN formatter)
├── graphify-out/          Knowledge graph (768 nodes, 1561 edges, 48 communities)
│   └── GRAPH_REPORT.md    Generated 2026-05-12; god nodes summary
├── docs/                  Design specs, UX change logs, plans
├── CLAUDE.md              Primary AI guidance file — load this first every session
├── CODING_ROADMAP.md      Phase-by-phase feature history + current focus
├── MAIN.md                Master architecture + tech-stack documentation
└── MAIN_FEATURES.md       Detailed per-feature documentation (large file)
```

*Citations: CLAUDE.md:7-11, MAIN.md:1-5, graphify-out/GRAPH_REPORT.md:1-6, apps/frontend/vite.config.js:14-16*

---

## 2. Backend Architecture

### Module Registration Order (`app.module.ts`)

| # | Module | Line | Responsibility | Key Service | Key Controller |
|---|--------|------|----------------|-------------|----------------|
| G | ConfigModule (global) | 27 | env vars everywhere | — | — |
| G | ThrottlerModule | 30 | global 100 req/60s rate limit | — | — |
| 1 | PrismaModule | 36 | DB access / circuit breaker | prisma.service.ts | — |
| 2 | SubscriptionModule | 37 | SaaS tiers, Stripe Billing | subscription.service.ts | subscription.controller.ts |
| 3 | AuthModule | 38 | JWT, Google OAuth, Email OTP, PIN login | auth.service.ts | auth.controller.ts |
| 4 | RestaurantsModule | 39 | Restaurant CRUD + Stripe Connect onboarding | restaurants.service.ts | restaurants.controller.ts |
| 5 | MenuModule | 40 | Category/item/option CRUD + translation pre-warm | menu-crud.service.ts | category.controller.ts, item.controller.ts, public-menu.controller.ts |
| 6 | OrdersModule | 41 | Order creation (server-side pricing), status workflow | orders.service.ts | orders.controller.ts |
| 7 | AssistanceModule | 42 | Call Waiter requests | assistance.service.ts | assistance.controller.ts |
| 8 | DashboardModule | 43 | Analytics + revenue trend (Luxon, restaurant timezone) | dashboard.service.ts | dashboard.controller.ts |
| 9 | TablesModule | 44 | Table status (parallel Promise.all), table orders | tables.service.ts | tables.controller.ts |
| 10 | HealthModule | 45 | Health check endpoint | — | health.controller.ts |
| 11 | FeedbackModule | 46 | Post-order ratings, Google Review redirect | feedback.service.ts | feedback.controller.ts |
| 12 | TranslationModule | 47 | DeepL auto-translate (platform-managed key) | translation.service.ts | — |
| 13 | StorageModule | 48 | Cloudflare R2 + sharp pipeline (WebP, 1200px/400px) | storage.service.ts | — |
| 14 | EventsModule | 49 | Socket.io gateway, restaurant rooms | — | events.gateway.ts |
| 15 | LoyaltyModule | 50 | FIFO point ledger, VIP tiers, expiry cron (midnight UTC) | loyalty.service.ts | — |
| 16 | PaymentModule | 51 | Stripe Connect pay-at-table, sessions, webhook | payment.service.ts | payment.controller.ts |
| 17 | MenuImportModule | 52 | OCR/CSV/JSON menu import via API key | menu-import.service.ts | menu-import.controller.ts |

*Citation: apps/backend/src/app.module.ts:26-53*

**Note:** CLAUDE.md:56 states CLAUDE.md module order as "Prisma, Auth, Restaurants, Menu, Orders, Assistance, Dashboard, Tables, Health, Feedback, Translation, Storage, Events, Payment, Loyalty" — the actual app.module.ts:37 shows SubscriptionModule now registered second (after PrismaModule, before AuthModule), and MenuImportModule added last. CLAUDE.md was not updated for this order change.

### Global Cross-Cutting Concerns

| Concern | Where | Details |
|---------|-------|---------|
| Auth strategy | `apps/backend/src/auth/jwt.strategy.ts` (file confirmed, not read in full) | JWT in httpOnly cookie, Bearer header fallback. Cookie name: `token`. `CLAUDE.md:59` |
| CSRF | `apps/backend/src/main.ts:62–104` | Double-submit cookie. Header `X-CSRF-Token` must match `csrf-token` cookie. Exempt: 6 public auth routes + webhook paths + GET/HEAD/OPTIONS + dev mode (`NODE_ENV !== 'production'`) |
| CORS | `apps/backend/src/main.ts:24–40` | `process.env.FRONTEND_URL`, `http://localhost:3001`, `http://127.0.0.1:3001`, `http://localhost:3002`, `http://127.0.0.1:3002`. `credentials: true` |
| Rate limiting | `apps/backend/src/app.module.ts:30–35` | ThrottlerGuard applied globally via `APP_GUARD`. 100 requests per 60,000 ms |
| API versioning | `apps/backend/src/main.ts:115–118` | `VersioningType.URI`, `defaultVersion: '1'`. All routes at `/api/v1/*` |
| Helmet CSP | `apps/backend/src/main.ts:45–60` | `defaultSrc: self`, `styleSrc: self + unsafe-inline + fonts.googleapis.com`, `frameSrc: self + stripe.com`, `connectSrc: self + ws: + wss:`, `crossOriginEmbedderPolicy: false` |
| Raw body | `apps/backend/src/main.ts:106–108` | `/api/v1/payments/webhook` and `/api/v1/subscription/webhook` get `express.raw`. All other routes get `express.json` (1mb limit) |

---

## 3. Frontend Architecture

### Route Table (`App.tsx`)

| Path | Component | Layout | Guard |
|------|-----------|--------|-------|
| `/` | HomePage | AppLayout | none |
| `/pricing` | PricingPage | AppLayout | none |
| `/login` | LoginPage | AppLayout | none |
| `/register` | RegisterPage | AppLayout | none |
| `/auth/callback` | OAuthCallbackPage | AppLayout | none |
| `/profile` | CustomerProfilePage | AppLayout | ProtectedRoute |
| `/dashboard` | DashboardPage | AppLayout | ProtectedRoute |
| `/dashboard/menu` | MenuEditorPage (+ MenuProvider) | AppLayout | ProtectedRoute |
| `/staff/pos` | PosPage (+ PosProvider) | POS Layout | StaffRoute |
| `/staff/kitchen` | KitchenPage | POS Layout | StaffRoute |
| `/device-enroll` | DeviceEnrollPage | bare | none |
| `/device-login` | DeviceLoginPage | bare | none |
| `/menu/public/:restaurantId` | PublicMenuPage | PublicLayout | none |
| `/checkout` | CheckoutPage | PublicLayout | none |
| `/order-confirmation` | OrderConfirmationPage | PublicLayout | none |
| `/feedback/:restaurantId` | FeedbackPage | PublicLayout | none |

*Citation: apps/frontend/src/App.tsx:64–163*

### Layouts

| Layout | Provider stack (outer→inner) | Purpose |
|--------|------------------------------|---------|
| AppLayout | SocketProvider → OrderProvider → AssistanceProvider → RestaurantProvider → NotificationProvider | Dashboard + owner routes with Header |
| PublicLayout | SocketProvider → RestaurantProvider → NotificationProvider → CartProvider | Customer-facing routes, no Header |
| POS Layout | SocketProvider → NotificationProvider → OrderProvider → RestaurantProvider → PosLayout | Fullscreen waiter POS |

*Citation: apps/frontend/src/App.tsx:34–62, 106–137*

### Context Providers

| Context | File | State owned | Mount scope |
|---------|------|-------------|-------------|
| AuthContext | `context/AuthContext.tsx` | current user, login/logout | Root (wraps Router + AuthProvider, line 68) |
| RestaurantContext | `context/RestaurantContext.tsx` | activeRestaurant, restaurant list | AppLayout, PublicLayout, POS Layout |
| MenuContext | `context/MenuContext.tsx` | menu categories + items | Dashboard menu editor only |
| CartContext | `context/CartContext.tsx` | cart items, table context | PublicLayout |
| OrderContext | `context/OrderContext.tsx` | live orders, socket-driven invalidation | AppLayout, POS Layout |
| AssistanceContext | `context/AssistanceContext.tsx` | assistance requests | AppLayout |
| SocketContext | `context/SocketContext.tsx` | socket.io connection, room join | AppLayout, PublicLayout, POS Layout |
| NotificationContext | `context/NotificationContext.tsx` | payment notifications, badge count | AppLayout, PublicLayout, POS Layout |
| PosContext | `context/PosContext.tsx` | in-memory POS cart (submitted/pending), session | POS Layout → PosPage only |

*Citation: apps/frontend/src/context/ (Glob enumeration), CLAUDE.md:117, apps/frontend/src/App.tsx:34–62*

### API Client (`apps/frontend/src/lib/api.ts`)

| Concern | Detail | Line |
|---------|--------|------|
| baseURL | `/api/v1` (same-origin, Vite proxy) | 11 |
| withCredentials | `true` — sends httpOnly cookie on every request | 15 |
| Bearer token interceptor | Attaches `Authorization: Bearer <token>` if `authToken` set | 190–195 |
| CSRF interceptor | Fetches `/api/v1/auth/csrf-token` once, attaches `X-CSRF-Token` on POST/PATCH/DELETE/PUT | 195–202 |
| 401 response interceptor | Redirects to `/login` on 401, skips `/auth/me` and `/auth/pin-login` to prevent loops | 204–224 |

---

## 4. Data Model

### Models (schema.prisma)

| Model | DB Table | Lines | Key fields |
|-------|----------|-------|------------|
| User | `app_user` | 10–30 | id (cuid), email (unique), password, pinHash, role (UserRole), restaurantId (staff link) |
| Restaurant | `restaurant` | 32–97 | id, name, country, ownerId, tier (SubscriptionTier), stripeCustomerId, stripeSubscriptionId, tierUpdatedAt, paymentsEnabled, isLoyaltyEnabled, loyaltyExchangeRate (default 10), loyaltyRedeemRate (default 150), timezone (default UTC), importApiKey (unique), defaultTheme |
| RestaurantTable | `restaurant_table` | 99–109 | id, name, restaurantId |
| MenuCategory | `menu_category` | 111–129 | id, name, restaurantId, order, availabilityType (AvailabilityType), startTime, endTime, daysOfWeek, translations (Json), imageUrl, thumbnailUrl, isDrinkCategory |
| MenuItem | `menu_item` | 132–158 | id, name, description, price (Float), currency (Currency), allergens, dietaryTags, imageUrl, thumbnailUrl, isOutOfStock, order, translations (Json), isFeatured, relatedItemIds, rewardPointsPrice |
| MenuOption | `menu_option` | 160–172 | id, name, type (OptionType), choices (Json — `[{name, priceModifier}]` — NO id field), menuItemId, translations (Json) |
| Order | `customer_order` | 174–200 | id, customerName, tableId, status (OrderStatus), restaurantId, totalPrice, tableSessionId, customerId, pointsEarned, pointsRedeemed |
| OrderItem | `order_item` | 202–212 | id, orderId, menuItemId, quantity, selectedOptions (Json) |
| AssistanceRequest | `assistance_request` | 214–225 | id, tableId, isResolved, restaurantId |
| DeviceEnrollmentToken | `device_enrollment_token` | 227–241 | id, tokenHash (unique), restaurantId, createdById, expiresAt, usedAt |
| Feedback | `feedback` | 243–256 | id, rating, comment, redirectedToGoogle, orderId (unique), restaurantId |
| LoyaltyAccount | `loyalty_account` | 258–272 | id, userId, restaurantId (unique pair), points, lifetimePoints |
| LoyaltyPointLedger | `loyalty_point_ledger` | 274–290 | id, loyaltyAccountId, orderId, type (LoyaltyPointTransactionType), points, remainingPoints, expiresAt, reminderSentAt |
| VerificationToken | `VerificationToken` (no @@map) | 333–344 | id, email, code (bcrypt-hashed), expiresAt, usedAt, attempts, lockedUntil |
| TableSession | `table_session` | 346–363 | id, token (unique cuid), tableId, restaurantId, status (TableSessionStatus) |
| Payment | `payment` | 365–383 | id, tableSessionId, restaurantId, stripePaymentIntentId (unique), amount, tipAmount, platformFeeAmount, currency, status (PaymentStatus), provider (PaymentProvider) |

*Citation: apps/backend/prisma/schema.prisma (full file read)*

### Enums

| Enum | Values | Lines |
|------|--------|-------|
| UserRole | OWNER, MANAGER, WAITER, KITCHEN, STAFF, CUSTOMER | 292–299 |
| Currency | EUR, BGN | 301–304 |
| OrderStatus | NEW, IN_PROGRESS, SERVED, CANCELED, COMPLETED | 306–312 |
| OptionType | VARIATION, ADDON | 314–317 |
| AvailabilityType | ALWAYS, SCHEDULED, HIDDEN | 319–323 |
| LoyaltyPointTransactionType | EARN, SIGNUP, REDEEM, EXPIRE, ADJUSTMENT | 325–331 |
| TableSessionStatus | OPEN, PAID, CLOSED_NO_PAYMENT | 385–389 |
| PaymentStatus | PENDING, SUCCEEDED, FAILED, REFUNDED | 391–396 |
| PaymentProvider | STRIPE, MYPOS, CASH | 398–402 |
| SubscriptionTier | FREE, STARTER, PROFESSIONAL, ENTERPRISE | 404–409 |

### Relations (ASCII — declared in schema only)

```
User ──── OwnedRestaurants ────────► Restaurant (1→many, via Restaurant.ownerId)
User ──── StaffRestaurant ──────────► Restaurant (many→1, via User.restaurantId → Restaurant.id)
User ────────────────────────────────► LoyaltyAccount (1→many)
User ────────────────────────────────► Order (1→many, customer relation)
User ────────────────────────────────► DeviceEnrollmentToken (createdBy, 1→many)

Restaurant ──────────────────────────► MenuCategory (1→many, cascade delete)
Restaurant ──────────────────────────► Order (1→many, cascade delete)
Restaurant ──────────────────────────► AssistanceRequest (1→many, cascade delete)
Restaurant ──────────────────────────► DeviceEnrollmentToken (1→many, cascade delete)
Restaurant ──────────────────────────► Feedback (1→many, cascade delete)
Restaurant ──────────────────────────► LoyaltyAccount (1→many, cascade delete)
Restaurant ──────────────────────────► RestaurantTable (1→many, cascade delete)
Restaurant ──────────────────────────► TableSession (1→many, cascade delete)
Restaurant ──────────────────────────► Payment (1→many, cascade delete)

MenuCategory ────────────────────────► MenuItem (1→many, cascade delete)
MenuItem ────────────────────────────► MenuOption (1→many, cascade delete)
MenuItem ────────────────────────────► OrderItem (1→many)

Order ───────────────────────────────► OrderItem (1→many, cascade delete)
Order ───────────────────────────────► Feedback (1→1)
Order ───────────────────────────────► LoyaltyPointLedger (1→many)
Order ──── tableSession ─────────────► TableSession (many→1)

RestaurantTable ─────────────────────► TableSession (1→many, cascade delete)
TableSession ────────────────────────► Payment (1→many, cascade delete)

LoyaltyAccount ──────────────────────► LoyaltyPointLedger (1→many, cascade delete)
```

---

## 5. Critical Data Flows

### Flow A: Customer Places Order from QR Menu

| Step | File | Line(s) | What happens |
|------|------|---------|--------------|
| 1 | Customer opens URL | — | `/menu/public/:restaurantId?table=<name>` → PublicMenuPage renders |
| 2 | `apps/frontend/src/pages/PublicMenuPage.tsx` | (file not read) | Fetches menu via `getMenu(restaurantId)` → `GET /api/v1/menu/public/:id` |
| 3 | `apps/frontend/src/lib/api.ts:48` | 48 | `createOrder(orderData)` → `POST /api/v1/orders` |
| 4 | Backend: orders.controller.ts → orders.service.ts | (service file not read in full) | Server-side price recalculation; choices validated by `c.name === selected.choiceName` (CLAUDE.md:105), `choice.priceModifier` read |
| 5 | `apps/backend/src/events/events.gateway.ts:80–93` | 80–93 | `emitToRestaurant` + `emitTableStatusChanged` called — emits `table:status-changed` to `restaurant_{restaurantId}` room |
| 6 | Dashboard SocketContext | (file not read) | Receives event → React Query invalidates `['tableStatuses']` |
| UNVERIFIED | Order:new event name | — | Exact socket event name emitted on new order creation not read in this session |

### Flow B: Owner Login → Dashboard → Live Tables

| Step | File | Line(s) | What happens |
|------|------|---------|--------------|
| 1 | `apps/frontend/src/lib/api.ts:28–31` | 28–31 | `login(email, password)` → `POST /api/v1/auth/login` |
| 2 | Backend sets httpOnly cookie `token` | CLAUDE.md:59 | JWT written to cookie. SameSite: lax. 1-day expiry |
| 3 | `apps/frontend/src/lib/api.ts:38–41` | 38–41 | `getCurrentUser()` → `GET /api/v1/auth/me` → AuthContext populates |
| 4 | RestaurantContext | (file not read in full) | Fetches restaurant list for authenticated owner |
| 5 | `apps/frontend/src/lib/api.ts:121–134` | 121–134 | `getTableStatuses(restaurantId)` → `GET /api/v1/tables/status/:restaurantId` → tables.service.ts `getTablesWithStatus()` runs two parallel queries (CLAUDE.md:65) |
| 6 | `apps/backend/src/events/events.gateway.ts:37–46` | 37–46 | Frontend SocketContext emits `joinRestaurantRoom`, server adds client to `restaurant_{restaurantId}` room |
| 7 | `apps/backend/src/events/events.gateway.ts:84–93` | 84–93 | On any table event → `table:status-changed` fires → React Query cache `['tableStatuses']` invalidated (CLAUDE.md:203) |

### Flow C: Stripe Connect Onboarding → Webhook → Restaurant Enabled

| Step | File | Line(s) | What happens |
|------|------|---------|--------------|
| 1 | `apps/frontend/src/lib/api.ts:281–284` | 281–284 | `generateStripeConnectLink(restaurantId)` → `POST /api/v1/restaurants/:id/stripe/connect` |
| 2 | RestaurantsService | (file not read) | Creates Stripe Connect account link → returns redirect URL |
| 3 | Owner completes Stripe OAuth flow | — | Stripe sends webhook to `/api/v1/payments/webhook` |
| 4 | `apps/backend/src/main.ts:106` | 106 | Raw body preserved for Stripe signature verification |
| 5 | `apps/backend/src/main.ts:76` | 76 | CSRF middleware skips `req.path === '/api/v1/payments/webhook'` |
| 6 | payment.controller.ts → payment.service.ts → StripeProvider | (files not read in full) | Webhook validates signature, sets `restaurant.stripeOnboarded = true`, `paymentsEnabled = true` |
| UNVERIFIED | paymentsEnabled update location | — | payment.service.ts handleWebhookEvent implementation not read in this session |

---

## 6. Realtime / Sockets

**Gateway file:** `apps/backend/src/events/events.gateway.ts` (lines 1–101)

**CORS on gateway:** `process.env.FRONTEND_URL || 'http://localhost:3001'`, `credentials: true`  
*(Citation: events.gateway.ts:15–17)*

### Rooms

| Room name | Join event | Leave event | Lines |
|-----------|-----------|-------------|-------|
| `restaurant_{id}` | `joinRestaurantRoom` | `leaveRestaurantRoom` | 37–62 |
| `order_{id}` | `joinOrderRoom` | — | 66–75 |

### Events Emitted (server → client)

| Event | Method | Payload | Lines |
|-------|--------|---------|-------|
| `table:status-changed` | `emitTableStatusChanged()` | `{ tableId, sessionId }` | 84–93 |
| Any custom event | `emitToRestaurant(restaurantId, eventName, payload)` | arbitrary | 80–82 |
| Any custom event | `emitToOrder(orderId, eventName, payload)` | arbitrary | 98–100 |

**Note:** `CLAUDE.md:63` states `payment:confirmed` is also emitted on successful payment — this call is inside `payment.service.ts` which was not read in this session. `emitToRestaurant` is the mechanism used.

### Frontend Reconnect

SocketContext connects via `io()` with no URL (same-origin proxy — CLAUDE.md:62). Vite dev server proxies `/socket.io` to backend (`vite.config.js:21–24`). On reconnect, React StrictMode causes left+rejoin (expected behavior per prior diagnosis).

---

## 7. SaaS Tiering

### Enum Values

`FREE | STARTER | PROFESSIONAL | ENTERPRISE`  
*(Citation: apps/backend/prisma/schema.prisma:404–409)*

### Backend Feature Map (`feature.service.ts`)

| Tier | Features | Lines |
|------|----------|-------|
| FREE | menu:view, menu:edit, qr:manage | 7–11 |
| STARTER | + menu:import, orders:receive, analytics:basic | 12–19 |
| PROFESSIONAL | + orders:call-waiter, analytics:full, payments:stripe, languages:multi, branding:custom, loyalty, customers:auth, upselling, dayparting | 20–36 |
| ENTERPRISE | `Object.values(FeatureFlag)` — all 22 flags | 37 |

*(Citation: apps/backend/src/subscription/feature.service.ts:6–38)*

### Staff Limits per Tier (`feature.service.ts:50–62`)

| Tier | Staff limit |
|------|-------------|
| FREE | 1 |
| STARTER | 1 |
| PROFESSIONAL | 5 |
| ENTERPRISE | Infinity |

### Where Tier is Read on Each Request

**Backend:**
- `apps/backend/src/subscription/feature.guard.ts` (read in prior session) — resolves restaurant from owner (`Restaurant.ownerId`) OR staff (`User.restaurantId`), calls `featureService.hasFeature(tier, flag)`, throws `403 FEATURE_LOCKED`
- `apps/backend/src/subscription/subscription.controller.ts:30–44` — `GET /subscription/status` resolves restaurant and returns tier + features

**Frontend:**
- `apps/frontend/src/hooks/useFeature.ts:100–112` — reads `RestaurantContext.activeRestaurant.tier`, returns boolean via local TIER_FEATURES map

### Architecture Risk — Dual TIER_FEATURES Map

Backend canonical map: `apps/backend/src/subscription/feature.service.ts:6–38`  
Frontend duplicate map: `apps/frontend/src/hooks/useFeature.ts:30–81`

These two maps are in sync as of this audit. ENTERPRISE tier: backend uses `Object.values(FeatureFlag)` (automatically includes new flags), frontend hardcodes all 22 flags explicitly. Adding a new flag to the enum requires manually updating `useFeature.ts` or it won't gate correctly on the frontend.

### Webhook Race Protection

`updateMany WHERE stripeCustomerId = X AND (tierUpdatedAt IS NULL OR tierUpdatedAt < eventTime)` — older events cannot overwrite newer tier state.  
*(Citation: apps/backend/src/subscription/subscription.service.ts:118–131)*

### Webhook Paths in CSRF Exempt List

`/api/v1/subscription/webhook` — explicitly checked in CSRF middleware.  
*(Citation: apps/backend/src/main.ts:77)*

Raw body also registered: `apps/backend/src/main.ts:107`

---

## 8. Security Posture

*(Reporting only what is actually configured in read files — no recommendations)*

| Concern | Configuration | Source |
|---------|--------------|--------|
| AuthN | JWT stored in httpOnly cookie (`token`). SameSite: lax. Bearer header fallback for cross-origin. | CLAUDE.md:59, main.ts:64 |
| Token storage | httpOnly cookie only. `api.ts` also maintains `authToken` module-level variable for Bearer header. No localStorage. | api.ts:5–8, CLAUDE.md:128 |
| CSRF | Double-submit cookie. `X-CSRF-Token` header must match `csrf-token` cookie. Enforced in prod only (`NODE_ENV !== 'production'` skips check). | main.ts:73–104 |
| CSRF exempt | 6 auth routes (login, register, otp/send, otp/verify, google, google/callback), both webhook paths, all GET/HEAD/OPTIONS | main.ts:65–80 |
| CORS | Origin allowlist: FRONTEND_URL + localhost:3001/3002. `credentials: true`. | main.ts:24–40 |
| Socket.io CORS | Same FRONTEND_URL restriction. `credentials: true`. | events.gateway.ts:14–18 |
| Helmet CSP | defaultSrc: self. scriptSrc: self (no unsafe-inline). styleSrc: unsafe-inline (for Tailwind). frameSrc: stripe.com. imgSrc: self + data + https. connectSrc: self + ws + wss. | main.ts:45–60 |
| Rate limiting | ThrottlerGuard: 100 req / 60 s globally. Per-endpoint throttle decorators possible (not verified in all controllers). | app.module.ts:30–35, CLAUDE.md:56 |
| Raw body | Two webhook paths get `express.raw`. All others get `express.json` 1mb limit. | main.ts:106–109 |
| CSRF dev bypass | `NODE_ENV !== 'production'` completely skips CSRF token validation. | main.ts:80 |
| OTP brute force | `VerificationToken` has `attempts`, `lockedUntil` fields. Rate limit at service layer (60s resend). | schema.prisma:338–340, CLAUDE.md:191 |
| Stripe webhook auth | `stripe.webhooks.constructEvent(rawBody, signature, secret)` — signature verified. | subscription.service.ts:79 |
| Secret loading | All credentials via `process.env.*`. No hardcoded production secrets found in read files. | main.ts, subscription.service.ts:5,10–12 |

---

## 9. Test Coverage Reality

### Backend Spec Files (16 files)

| File | Area |
|------|------|
| `app.controller.spec.ts` | Root controller |
| `auth.service.spec.ts` | Auth |
| `restaurants-stripe.service.spec.ts` | Stripe Connect |
| `loyalty-ledger.utils.spec.ts` | Loyalty ledger ops |
| `loyalty-tiers.utils.spec.ts` | Loyalty tier logic |
| `stripe.provider.spec.ts` | Stripe provider |
| `payment-history-query.dto.spec.ts` | Payment DTO |
| `menu-crud.service.spec.ts` | Menu CRUD |
| `tables.service.spec.ts` | Tables (19 tests per CLAUDE.md:255) |
| `users.service.spec.ts` | Users (17 tests per CLAUDE.md:255) |
| `translation.service.spec.ts` | Translation (14 tests per CLAUDE.md:255) |
| `orders.service.spec.ts` | Orders |
| `payment.service.spec.ts` | Payment |
| `feature.guard.spec.ts` | Feature guard |
| `feature.service.spec.ts` | Feature service |
| `subscription.service.spec.ts` | Subscription |

*Citation: apps/backend/src/**/*.spec.ts (Glob)*  
Total reported by CLAUDE.md:255: 122 tests across these files.

### Frontend Test Files (5 files)

| File | Area |
|------|------|
| `context/AuthContext.test.tsx` | Auth context |
| `App.test.tsx` | App root |
| `components/staff/StaffCreatedModal.test.tsx` | Staff modal |
| `pages/Dashboard/SettingsView.test.tsx` | Settings page |
| `lib/__tests__/currency.test.ts` | Currency formatter |

*Citation: apps/frontend/src/**/*.test.{ts,tsx} (Glob)*

### Test Runner Config

- **Backend:** Jest, co-located `*.spec.ts`. E2E config: `apps/backend/test/jest-e2e.json`. E2E requires `npm run test:prepare` (copies `.env.test` → `.env`). *(CLAUDE.md:275–276)*
- **Frontend:** Vitest + jsdom. Config in `vite.config.js:31–38`. Custom reporter: `tdd-guard-vitest`.

**Coverage % not measured in this session.** Run `npm run test:cov` in `apps/backend` for backend coverage.

---

## 10. Shipped vs Planned

*Reproducing from CLAUDE.md and CODING_ROADMAP.md — no interpretation.*

### Shipped (CODING_ROADMAP.md header, lines 1–18)

| Sprint | Status |
|--------|--------|
| V1 MVP (Phases 1–8) | ✅ Complete (April 9, 2026) |
| V2 Premium (Phases 9–14) | ✅ Complete |
| V2.5 Visual Polish + Branding (Phases 15–17 + Mobile UX) | ✅ Complete |
| UI/UX Audit & Theme Polish | ✅ May 4, 2026 |
| Analytics & Translation Overhaul | ✅ May 5, 2026 |
| Customer Auth (Email OTP) + Bug Fixes | ✅ May 6, 2026 |
| OCR Import Integration | ✅ May 9, 2026 |
| Waiter POS (`/staff/pos`) | ✅ May 9–10, 2026 |
| Security Hardening (Phase 21) | ✅ May 10–11, 2026 |
| Staff Roles & RBAC (Phase 18) | ✅ May 12–14, 2026 |
| Public Menu Mobile UX Redesign | ✅ May 15, 2026 |
| Stripe Connect Payments (Phase 19) | ✅ May 8, 2026 |
| Code Review & PR#3 Fixes | ✅ May 15, 2026 |
| Security & Bug Fixes | ✅ May 15, 2026 |
| Infrastructure & Polish Sprint | ✅ May 15, 2026 |
| SaaS Tiering V2 | ✅ May 16, 2026 |

### Planned

| Phase | Status |
|-------|--------|
| Phase 20 — Multi-location (menu templates, bulk price updates, cross-location analytics) | Planned (Current Focus) |
| Phase 18 (partial) | Paused |
| V4 Enterprise (AWS/GCP, Redis, POS integrations, React Native staff app) | Planned |

*Citation: CODING_ROADMAP.md:1–18, CLAUDE.md:267–270*

---

## 11. Risks & Debt

*Only items explicitly flagged in code comments or CLAUDE.md "gotchas" / "do not" sections.*

### Code Comments (TODO/FIXME/HACK/XXX)

| File | Line | Comment |
|------|------|---------|
| `apps/backend/src/loyalty/loyalty.service.ts` | 316 | `// TODO: replace with actual email/push delivery here` — candidate struct built but delivery method incomplete (email is now sent via Resend per CLAUDE.md:243, but this comment remains in code) |

*Citation: loyalty.service.ts:316 (confirmed via Grep)*

### CLAUDE.md "Gotchas" / "do not" Invariants

| Risk | Rule | Source |
|------|------|--------|
| Cross-origin cookie breakage | NEVER change `api.ts` baseURL to read from `VITE_API_URL` directly | CLAUDE.md:127 |
| Token leak | NEVER read token from localStorage — token in httpOnly cookie only | CLAUDE.md:128 |
| Menu option choices schema | `choices` JSON must be `[{name, priceModifier}]` — no `id` field, no `price` key. Wrong key → "Invalid choice selected" on every order | CLAUDE.md:101–106 |
| Prisma transaction writes | Never use `Promise.all` over Prisma writes inside `$transaction` — use `updateMany` | CLAUDE.md:75 |
| Loyalty tier thresholds | Never hardcode 500/2000 thresholds or 1.2/1.5 multipliers — read from Restaurant row via `loyalty-tiers.utils.ts` | CLAUDE.md:74 |
| Loyalty exchange rate | `loyaltyExchangeRate` has `@Max(100)` in DTO — do not remove. Past bug: absurdly high point awards | CLAUDE.md:90–92 |
| Windows clean script | Backend `clean` uses `rmdir /s /q` — fails cross-platform. Use `rm -rf dist` manually on Linux/Mac | CLAUDE.md:123 |
| CSRF middleware ordering | Must be: Helmet → cookieParser → CSRF → useGlobalPipes. Wrong order breaks cookie reading | CLAUDE.md:129 |
| DeepL API key | `restaurant.deeplApiKey` column exists but is NEVER read or written. Platform manages key via env var. Do not add call-sites | CLAUDE.md:66 |
| Translation service signature | `translateTexts/translateText/translateObject` take NO `apiKey` param | CLAUDE.md:66 |

### Architectural Risks (from graphify + code reads, not invented)

| Risk | Evidence |
|------|---------|
| Stripe API version `'2026-04-22.dahlia'` in subscription.service.ts | subscription.service.ts:6 — no such Stripe API version exists in documented releases. `'2025-10-29.acacia'` or `'2024-12-18.acacia'` are recent real versions. This may cause Stripe SDK to reject or behave unexpectedly. |
| `useFeature.ts` duplicates `TIER_FEATURES` map | useFeature.ts:30–81 hardcodes same map as feature.service.ts:6–38. ENTERPRISE: backend auto-expands via `Object.values(FeatureFlag)`, frontend hardcodes 22 flags — new flags added to enum won't gate on frontend without updating this file |
| `FeatureService.getStaffLimit()` declared but staff creation path not verified | feature.service.ts:50–62 exists but whether `staff.service.ts` or `auth.service.ts` calls it on create was not confirmed in files read this session |
| VerificationToken has no `@@map` | schema.prisma:333 — defaults to table name `"VerificationToken"` (PascalCase), inconsistent with all other tables using snake_case `@@map` |
| Stripe webhook `event.rawBody!` null assertion | subscription.controller.ts:68 uses non-null assertion on `req.rawBody` — if middleware misconfigured, throws at runtime |

---

## 12. Open Questions (UNVERIFIED)

- **UNVERIFIED:** Exact socket event name emitted by `OrdersService.create` on new order creation. `CLAUDE.md:63` says `emitTableStatusChanged` is called from `OrdersService.create`, `OrdersService.updateStatus`, `PaymentService.handleWebhookEvent`, and `PaymentService.closeSession` — but the event for "new order received" visible to dashboard staff was not confirmed (could be via `emitToRestaurant` with a custom event name not read in this session).

- **UNVERIFIED:** Whether `PaymentService.handleWebhookEvent` sets `restaurant.paymentsEnabled = true` and `stripeOnboarded = true` on Stripe Connect completion. CLAUDE.md documents this behavior (CLAUDE.md:238–239) but `payment.service.ts` was not read in full.

- **UNVERIFIED:** Whether `@RequireFeature` decorator is applied to any controller endpoints beyond SubscriptionController. `feature.guard.ts` exists and was confirmed in session context, but grep for decorator usage across all controllers was not run.

- **UNVERIFIED:** Whether `SubscriptionTier.stripePriceId` field (schema.prisma:82) is ever written — `subscription.service.ts` updates `stripeSubscriptionId` (line 128) but not `stripePriceId`. May be an unused field.

- **UNVERIFIED:** `MAIN_FEATURES.md` was not fully read — file exceeds safe read window. Key architectural facts were sourced from CLAUDE.md, MAIN.md, and CODING_ROADMAP.md instead.

- **UNVERIFIED:** Whether `Restaurant.deeplApiKey` column ever returns data in any API response (select clauses in restaurants.service.ts not confirmed). Per CLAUDE.md:66, it should never be read or written, but no automated enforcement exists.

- **UNVERIFIED:** Whether the `tdd-guard-vitest` reporter in `vite.config.js:36` enforces coverage thresholds or just reports. `tdd-guard-vitest` is a custom/community plugin — its behavior depends on configuration not visible in this file.

---

*End of audit. 12 sections complete. No sections stopped due to evidence gaps — all INSUFFICIENT EVIDENCE items moved to Open Questions.*
